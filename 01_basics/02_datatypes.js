"use strict"; // treat all JS code as newer version

//alert(3+4) -> we are using node.js and not browser

console.log(
    3 
    + // -> X code readability should be maintained
    6
)

//documents to remember tc39 and MDN

//primitive datatypes
//number, bigint, string, boolean(true / false), null(stand alone value) [value is empty], undefined, symbol(uniqueness)

//object

let name = "Rakshitha"
let age = 22
let isLoggedIn = false
let state
let status = null

console.log(typeof "Rakshi")
console.log(typeof null)
console.log(typeof undefined)